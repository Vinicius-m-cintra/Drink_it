import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginVertical: 5,
  },
  checked: {
    width: 15,
    height: 15,
    borderWidth: 1,
    borderColor: '#fff',
    borderRadius: 2,
    backgroundColor: '#fff',
  },
  text: {
    color: '#fff',
    marginLeft: 5,
  },
});

export default styles;
